package com.example.first // Make sure this matches your AndroidManifest.xml package

import android.os.Bundle
import io.flutter.embedding.android.FlutterFragmentActivity // Import FlutterFragmentActivity

class MainActivity: FlutterFragmentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // You can add additional setup code here if needed
    }
}
